# DecayMixer

This code implements an experimental token mixer that uses only dynamic decays, allowing parallel training with linear complexity and optimal performance in long contexts.

## License

MIT (original code) + AGPL v3+ (new/modified code).